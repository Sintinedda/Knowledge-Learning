{% extends 'base.html.twig' %}

{% block title %}Edit Lesson{% endblock %}

{% block body %}
    <h1>Éditer {{ lesson.name }}</h1>

    {{ include('lesson/_form.html.twig', {'button_label': 'Éditer'}) }}

    <a href="{{ path('app_theme_index') }}">Retour à la liste</a>

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
