{% extends 'base.html.twig' %}

{% block title %}Edit Theme{% endblock %}

{% block body %}
    <h1>Éditer {{ theme.name }}</h1>

    {{ include('theme/_form.html.twig', {'button_label': 'Éditer'}) }}

    <a href="{{ path('app_theme_index') }}">Retour à la liste</a>

    {{ include('theme/_delete_form.html.twig') }}
{% endblock %}
